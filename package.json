{
  "name": "tsrpc-protocol",
  "version": "0.2.0",
  "description": "Base protocol for tsrpc",
  "scripts": {
    "build": "rm -rf dist && tsc -d && cp package.json LICENSE README.md dist/ && cp *.d.ts dist/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/k8w/tsrpc-protocol.git"
  },
  "keywords": [
    "k8w",
    "tsrpc"
  ],
  "author": "k8w",
  "license": "Apache-2.0"
}
